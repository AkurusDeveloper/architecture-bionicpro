server:
  port: 8085
  servlet:
    session:
      cookie:
        secure: false
        http-only: true
        same-site: lax

spring:
  application:
    name: bionicpro-ui

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8

  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: ${KEYCLOAK_CLIENT_ID:bionicpro-ui}
            client-secret: ${KEYCLOAK_CLIENT_SECRET:}
            scope:
              - openid
              - profile
              - email
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: BionicPRO UI
        provider:
          keycloak:
            # issuer-uri не указываем, т.к. все endpoints указаны явно
            # Это предотвратит использование discovery endpoint и проблему с валидацией issuer
            authorization-uri: ${KEYCLOAK_AUTHORIZATION_URI:http://localhost:8080/realms/reports-realm/protocol/openid-connect/auth}
            token-uri: ${KEYCLOAK_TOKEN_URI:http://keycloak:8080/realms/reports-realm/protocol/openid-connect/token}
            # user-info-uri не указываем - используем информацию из ID token
            # user-info-uri: ${KEYCLOAK_USERINFO_URI:http://keycloak:8080/realms/reports-realm/protocol/openid-connect/userinfo}
            jwk-set-uri: ${KEYCLOAK_JWK_SET_URI:http://keycloak:8080/realms/reports-realm/protocol/openid-connect/certs}
            user-name-attribute: preferred_username

bionicpro:
  api:
    base-url: ${REPORTS_API_URL:http://localhost:8090/api}
    timeout-seconds: 30

logging:
  level:
    root: INFO
    com.bionicpro: DEBUG
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always


